[build]
builder_version = "2.2.0"

# Railway will automatically use Dockerfile if present
# These settings are fallbacks if Dockerfile is not used

[build.railpack]
install = "composer install --no-dev --optimize-autoloader"

build = """
npm install && \\
npm run build
"""

postBuild = """
php artisan config:cache && \\
php artisan route:cache
"""

[deploy]
start_command = "php artisan serve --host=0.0.0.0 --port=$PORT"
healthcheck_path = "/"
healthcheck_timeout = 100
restart_policy_type = "ON_FAILURE"
restart_policy_max_retries = 3

# Docker-specific configuration
# When Dockerfile is present, Railway uses it for building
# The start command above is used for the running container

[environments.production]
APP_ENV = "production"
APP_DEBUG = "false"
LOG_CHANNEL = "errorlog"
SESSION_DRIVER = "cookie"
SESSION_SECURE_COOKIES = "false"
DB_CONNECTION = "sqlite"
DB_DATABASE = "/app/data/database.sqlite"

[watch]
ignorePaths = [
    ".git",
    "node_modules",
    "vendor",
    ".railway"
]
